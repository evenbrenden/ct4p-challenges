2a. Partial order because every a is an actual b. Not total becuase not all bs are as, so there is not a relation between all elements.

2b. Partial order because if T1 is a subtype of T2 and vica versa than T1 is the same type as T2. Not total because there may exist types that are not in relation to each other.

3. AND: mempty = True
        mappend = &&
   OR:  mempty = False
        mappend = ||

